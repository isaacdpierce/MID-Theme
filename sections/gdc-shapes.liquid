{%- assign ss = section.settings -%}

<section class="gdc-shapes__wrapper section--{{ section.id }}">
  SHAPES 
  <div style="width: 100px; height: 100px; background-color: blueviolet;" >

  </div>
  {% for block in section.blocks %}
  {%- assign bs = block.settings -%}
    {% if block.type == "shape" %}
      <div class="gdc-shape block--{{ block.id }}"></div>
      <style>
        .gdc-shape.block--{{ block.id }} {
          width: {{ bs.width }}px;
          height: {{ bs.height }}px;
          background-color: {{ bs.bg_color }};
          margin-right: {{ bs.spacer }}px;
          border-radius: {{ bs.border_radius }}px;
        }
       </style>
    {% endif %}
  {% endfor %}

</section>

<style>
  .gdc-shapes__wrapper.section--{{ section.id }} {
    display: flex;
    margin-top: {{ ss.t_margin }}px;
    margin-bottom: {{ ss.b_margin }}px;
    padding-top: {{ ss.t_padding }}px;
    padding-bottom: {{ ss.b_padding }}px;
    background-color: {{ ss.bg_color }};
  }

</style>

{% schema %}
  {
    "name": "GDC: Shapes",    
    "settings": [
      {
        "type": "header",
        "content": "Section Settings" 
      },
      {
        "type": "range",
        "id": "t_margin",
        "label": "Section Top margin",
        "default": 0,
        "min": 0,
        "max": 200,
        "step": 10,
        "unit": "px" 
      },
      {
        "type": "range",
        "id": "b_margin",
        "label": "Section Bottom margin",
        "default": 0,
        "min": 0,
        "max": 200,
        "step": 10,
        "unit": "px"
      },
      {
        "type": "range",
        "id": "t_padding",
        "label": "Section Top padding",
        "default": 20,
        "min": 0,
        "max": 200,
        "step": 10,
        "unit": "px" 
      },
      {
        "type": "range",
        "id": "b_padding",
        "label": "Section Bottom padding",
        "default": 20,
        "min": 0,
        "max": 200,
        "step": 10,
        "unit": "px"
      },
      {
        "type": "color",
        "id": "bg_color",
        "label": "Background Color",
        "default": "#ededed"
      }
    ],
    "blocks": [
      {
        "type": "shape",
        "name": "Shape",
        "settings": [
          {
            "type": "color",
            "id": "bg_color",
            "label": "Background Color",
            "default": "#297F96"
          },
          {
            "type": "range",
            "id": "width",
            "label": "Shape Width",
            "default": 50,
            "min": 10,
            "max": 350,
            "step": 10,
            "unit": "px" 
          },
          {
            "type": "range",
            "id": "height",
            "label": "Shape Height",
            "default": 50,
            "min": 10,
            "max": 350,
            "step": 10,
            "unit": "px" 
          },
          {
            "type": "range",
            "id": "spacer",
            "label": "Space between shapes",
            "default": 10,
            "min": 10,
            "max": 40,
            "step": 1,
            "unit": "px" 
          },
          {
            "type": "range",
            "id": "border_radius",
            "label": "Rounded Corners",
            "default": 0,
            "min": 0,
            "max": 50,
            "step": 1,
            "unit": "px" 
          }
        ]
      }
    ],
    "presets": [
      {
        "name": "GDC: Shapes",
        "category": "GDC Custom Components",
        "blocks": []
      }
    ]
  }
{% endschema %}